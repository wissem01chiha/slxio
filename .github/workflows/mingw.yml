name: MinGW
on:
  push:
  pull_request:
  workflow_call:
  workflow_dispatch:

defaults:
  run:
    shell: bash

jobs:
  cmake:
    name: ${{matrix.name}}
    runs-on: ${{matrix.os}}
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        os: 
          - windows-latest
        name: 
          - MinGW
        toolchain:
          - windows-mingw
        configuration:
          - Release
          - Debug
        compiler: 
          - gcc
        cxx-compiler: 
          - g++
        python-version:
          - 3.12

    steps:
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure
        run: >
          cmake -S . -B build
          -G Ninja
          -DCMAKE_CXX_STANDARD=14
          -DCMAKE_BUILD_TYPE=${{ matrix.configuration }}
          -DPython_EXECUTABLE=${{ steps.setup-python.outputs.python-path }} 
        env:
          CC: ${{ matrix.compiler }}
          CXX: ${{ matrix.cxx-compiler }}

      - name: Build
        run: cmake --build build
        
      - name: Upload Build Folder
        uses: actions/upload-artifact@v4
        with:
          name: build-${{ matrix.name }}
          path: build/

      - name: Test
        run: ctest -C ${{ matrix.configuration }} --parallel 4 --output-on-failure --max-width 150
        working-directory: build
